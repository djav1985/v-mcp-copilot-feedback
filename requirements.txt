# VibeCode Feedback Server Dependencies with MCP Python SDK
mcp>=1.14.1  # Official MCP Python SDK
pushover>=0.5  # Pushover notifications
uvicorn>=0.35.0  # ASGI server for web interface
starlette>=0.48.0  # Web framework for human interface
# Python 3.8+ required for asyncio and dataclass features